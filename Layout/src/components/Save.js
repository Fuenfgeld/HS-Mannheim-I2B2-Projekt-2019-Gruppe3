import React, {Component} from "react";

class Save extends Component{
    constructor(props) {
        super(props);
        this.state = {

        }
    }

    render() {
        return(
                <button onClick= "myFunction()">Save</button>
        )
    }
}

export default Save;